export const allTasks = () => {
  return {
    type: 'ALL_TASKS',
  }
}

export const pendingTasks = () => {
  return {
    type: 'PENDING_TASKS',
  }
}

export const completeTasks = () => {
  return {
      type: 'COMPLETE_TASKS',
  }
}